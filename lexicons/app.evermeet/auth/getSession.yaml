lexicon: 1
defs:
  main:
    type: query
    description: Get information about the current auth session. Requires auth.
    output:
      encoding: application/json
      schema:
        type: object
        properties:
          handle:
            type: string
            format: handle
          did:
            type: string
            format: did