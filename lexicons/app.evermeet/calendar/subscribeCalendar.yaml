lexicon: 1
defs:
  main:
    type: procedure
    input:
      encoding: application/json
      schema:
        type: object
        required: ["did"]
        properties:
          did:
            type: string
            format: did
            example: did:mock:web3privacy
    output:
      encoding: application/json
      schema:
        type: object