lexicon: 1
defs:
  main:
    type: query
    parameters:
      type: params
      required: ["did", "cid"]
      properties:
        did:
          type: string
          format: did
        cid:
          type: string
    output:
      encoding: "*/*"