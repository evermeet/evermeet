lexicon: 1
defs:
  main:
    type: query
    parameters:
      type: params
      properties:
        repo:
          type: string
          format: did
          example: did:web:evermeet.local
    output:
      encoding: application/json
      schema:
        type: object