lexicon: 1
defs:
  main:
    type: query
    parameters:
      type: params
      required: ["col"]
      properties:
        col:
          type: string
          example: events
    output:
      encoding: application/json
      schema:
        type: object