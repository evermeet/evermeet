lexicon: 1
defs:
  main:
    type: query
    parameters:
      type: params
      required: ["repo", "room"]
      properties:
        room:
          type: string
          example: testRoomId
    output:
      encoding: application/json
      schema:
        type: object