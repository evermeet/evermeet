// Package version holds the single release label for binaries and APIs.
package version

// Version is the Evermeet release identifier (shown in logs, /health, admin API).
const Version = "v0.1.0-alpha.4"
