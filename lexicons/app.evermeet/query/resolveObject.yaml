lexicon: 1
defs:
  main:
    type: query
    parameters:
      type: params
      required: ["id"]
      properties:
        id:
          type: string
    output:
      encoding: application/json
      schema:
        type: object
        required: ["type", "item"]
        properties:
          type:
            type: string
            enum:
              - event
              - calendar
          item:
            type: object
    errors:
    - NotFound  